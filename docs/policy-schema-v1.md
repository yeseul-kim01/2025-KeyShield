# Policy Schema v1 (Draft)

KeyShield policies are defined as JSON documents with:
- priority-based rule evaluation
- conflict resolution (strongest decision wins)
- optional TTL for temporary overlays

This document will be finalized alongside /policy/decide API.
